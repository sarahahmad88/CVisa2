# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.Python
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Streamlit secrets — never commit real API keys
.streamlit/secrets.toml

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Caches
.cache/
*.log
.ipynb_checkpoints/

# Local FAISS / embedding caches (rebuilt automatically at startup)
*.faiss
*.index

# Runtime usage stats (visits / token usage / ratings) — generated automatically
data/usage_stats.json
